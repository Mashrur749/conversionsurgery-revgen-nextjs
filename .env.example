# === DATABASE ===
# Neon PostgreSQL connection string
# Get from: https://console.neon.tech
DATABASE_URL=postgresql://user:password@ep-xxx.region.aws.neon.tech/dbname?sslmode=require

# === AUTHENTICATION (NextAuth v4) ===
# Secret for encrypting sessions (generate: openssl rand -base64 32)
# Required: YES
AUTH_SECRET=your-secret-key-here

# Base URL for auth redirect (auto-detected in most environments)
# Required: Only if behind a proxy
AUTH_URL=http://localhost:3000

# Email display name for magic links
# Format: "Display Name <email@example.com>"
EMAIL_FROM="Revenue Recovery <noreply@contact.getconversionsurgery.com>"

# === TWILIO (SMS WEBHOOKS) ===
# Get from: https://www.twilio.com/console
TWILIO_ACCOUNT_SID=your-account-sid
TWILIO_AUTH_TOKEN=your-auth-token
TWILIO_NUMBER=+1234567890

# === OPENAI (AI RESPONSES) ===
# API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# === RESEND (EMAIL SERVICE) ===
# API key from: https://resend.com/api-keys
RESEND_API_KEY=re_...

# === CRON JOBS ===
# Secret for authorizing scheduled tasks
# Generate: openssl rand -base64 32
CRON_SECRET=your-cron-secret-key

# === APPLICATION ===
# Public URL for the app
# Used in magic links, webhooks, etc.
NEXT_PUBLIC_APP_URL=https://your-domain.com
